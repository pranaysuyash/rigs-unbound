# Patchwork Tractor Intake — 2026-07-25

Status: **admitted reference; reconstruction not implemented**

This directory records the `img2threejs` intake evidence for the generated isolated tractor reference:

- Source reference: [`tractor_isolated_reconstruction_reference_2026-07-25.png`](../../../exploration/assets/references/tractor_isolated_reconstruction_reference_2026-07-25.png)
- Technical probe: passed (`1747 × 900`, no warnings)
- Reference admission: passed as `isolated-three-quarter`
- Foreground coverage: `0.2924`
- Largest connected component fraction: `1.0`
- pHash: `13026260861505449107`
- SHA-256: `eb42e00ef63038773db5c45e4d496e3a75a4ac634e0a03980cfe4cfc8b228900`

## Artifacts

- `pre-spec-assessment.json`: generated complex-quality assessment skeleton. Its observed object class, proportions, component counts, and unknowns still require human authoring.
- `detail-inventory.json`: 3×3 crop scaffold. The detail stubs are intentionally not treated as completed until each observed feature is named, confidence-scored, and mapped to a real component or material override.
- `detail-zones/`: persistent crop evidence for the detail-inventory pass.
- `object-sculpt-spec.json`: generated starter spec. It is **not strict-quality validated** and must not be used to generate runtime code in its current state.
- `RECONSTRUCTION_PLAN.md`: authored component, detail, material, action-readiness, and review-gate proposal for discussion before strict spec completion.
- `object-sculpt-spec-authored.json`: authored complex spec; `validate_sculpt_spec.py --strict-quality` passes with zero errors. Non-blocking warnings remain for per-component `actionProfile` metadata.
- `generated/createPatchworkTractorModel.ts`: blockout factory generated by the upstream pass-gated generator. It is an intake artifact, not a project runtime import.
- `preview/`: preview-only browser surface for the generated factory. It is not imported by the game renderer.
- `blockout-front-three-quarter.png`: 1440×900 browser capture using the plain evaluation renderer.
- `blockout-comparison-sheet.png`: reference/render evidence sheet for visual review.

## Gate status

The source is now eligible for the next `img2threejs` stage, but code generation remains blocked until:

1. object class, silhouette, component hierarchy, and material families are authored from direct inspection;
2. at least 10 identity-defining details are mapped to component/material fields;
3. the complex spec passes structural and `--strict-quality` validation;
4. multiple review viewpoints and action-ready sockets/pivots are defined;
5. the project owner accepts the single-image uncertainty and the generated-reference terms for the intended use.

Reference-derived PBR evidence passes the tool threshold at confidence `0.86`, but the tool explicitly caps this as a single-image estimate; it still requires browser render review.

## Current build gate

The generator's `blockout` output has now been rendered in the isolated browser preview. The generator was corrected so authored dimensions are emitted on meshes rather than compounded through parent pivots, and root-pivot geometry is hidden from the render; the browser now reports stable bounds `[-2.1, -1.2, -1.05]` to `[2.1, 2.55, 1.05]` with no console warnings. The upstream Tier 1 diagnostic still **fails** for the current coarse output: silhouette IoU `0.5112` (required `0.85`), aspect-ratio delta `0.7506` (required `0.05`), and scale delta `0.1431` (required `0.08`). The comparison sheet confirms that the remaining chassis/body boxes are not a recognizable tractor. These are meaningful generator corrections, not an authorization to advance the pass.

The previous browser texture-update warnings were removed at the skill source by allowing `TextureLoader` to own its update lifecycle. The current capture has zero console errors or warnings.

No browser integration or later sculpt pass has been performed. The next implementation should refine the factory/spec until the blockout has the tractor's major silhouette and wheel/hood/cab separation, then rerun the same evidence loop.

No GLB, procedural tractor factory, loader, or renderer integration was created from this intake.
